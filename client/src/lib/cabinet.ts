// The console and the boombox are the same 72x49 map size, so one width rule
// fits both and a test asserts the two maps stay that way.
export const CABINET_WIDTH =
  "w-[min(100%,calc((100svh-16rem)*72/49))]";
